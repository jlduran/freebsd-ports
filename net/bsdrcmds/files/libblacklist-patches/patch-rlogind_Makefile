--- rlogind/Makefile.orig	2026-05-07 18:30:51 UTC
+++ rlogind/Makefile
@@ -13,9 +13,9 @@ CFLAGS+= -DINET6
 CFLAGS+= -DINET6
 .endif
 
-.ifdef USE_LIBBLOCKLIST
-CFLAGS+=	-DUSE_BLOCKLIST
-LDADD+=		-lblocklist
+.ifdef USE_LIBBLACKLIST
+CFLAGS+=	-DUSE_BLACKLIST
+LDADD+=		-lblacklist
 .endif
 
 .include <bsd.prog.mk>
